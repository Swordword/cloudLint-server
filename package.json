{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm run socket:dev\" \"npm run app:dev\"",
    "socket:dev": "cd socket && node index.js",
    "app:dev": "cd app && npm run dev"
  },
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}